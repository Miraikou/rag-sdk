import { describe, it, expect } from 'vitest';

describe('冒烟测试', () => {
  it.todo('should import all packages without errors');
  it.todo('should create pipeline with default config');
  it.todo('should ingest a simple document and query');
});
