import { describe, it, expect } from 'vitest';

describe('IndexingPipeline', () => {
  it.todo('should index documents with all stages enabled');
  it.todo('should index documents with only required stages');
  it.todo('should reindex a single document');
  it.todo('should throw if loader is missing for indexFromSource');
});
